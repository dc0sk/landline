#!/usr/bin/env sh
# landline pre-push hook.
# Enable once per clone with:  git config core.hooksPath .githooks
#
# Runs today (docs-only repo):
#   python3 scripts/trace-gate.py  — full requirement -> test traceability gate
#   (R3/R4): fails on any TC naming an unknown requirement, or any uncovered
#   M/S requirement.
#
# The Rust steps below are commented out until the Axum/Tokio workspace lands.
set -eu

echo "[pre-push] python3 scripts/trace-gate.py (R3/R4 gate)"
python3 scripts/trace-gate.py

# --- Rust workspace steps (enable once the workspace lands) -------------------
# echo "[pre-push] cargo test --workspace"
# cargo test --workspace
# echo "[pre-push] cargo audit"
# cargo audit

echo "[pre-push] OK"
